#pragma once
#include "Texture.h"
#include "Model.h"

class TexturesAndModels
{
public:
    static Model* carrot;
    static Texture* carrotTex;
    static Model* bed;
    static Texture* soilTex;
    static Model* sun;
    static Texture* sunTex;
    static Model* bunny;
    static Texture* bunnyTex;
};

